import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Colors.blue;
  static const Color accent = Colors.blueAccent;
  static const Color scheduledCard = Colors.orangeAccent;
  static const Color publishedCard = Colors.green;
  static const Color failedCard = Colors.redAccent;
}
